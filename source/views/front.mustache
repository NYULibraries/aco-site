<!doctype html>
<html>
<head>{{> head }}</head>
<body data-app="{{ appUrl }}" data-appRoot="{{ appRoot }}">
  {{> header }}
  {{> search_form }}
  <h2>{{ appName }} : {{ title }}</h2>
  {{> footer }}
</body>
</html>