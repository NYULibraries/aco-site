<!doctype html>
<html>
<head>{{> head }}</head>
<body data-app="{{ appUrl }}">
  {{> header }}
  <a href="{{ appUrl }}">Home</a> &gt; {{ title }}
  <h1>{{ title }}</h1>
  {{> footer }}
  {{{ javascript }}}
</body>
</html>