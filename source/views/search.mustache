<!doctype html>
<html>
<head>{{> head }}</head>
<body data-app="{{ appUrl }}">
  {{> header }}
  <h2>{{ title }}</h2>
  <span><a href="{{ appUrl }}" title="{{ appName }}">{{ appName }}</a> &gt; {{ title }}</span>
  {{> search_form }}
  {{> items }}
  {{> partners }}
  {{> footer }}
</body>
</html>