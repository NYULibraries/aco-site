<!doctype html>
<html>
<head>{{> head }}</head>
<body data-app="{{ appUrl }}" data-appRoot="{{ appRoot }}" class="{{ bodyClass }}">
  {{> header }}
  <main class="main container-fluid" role="main">
    <header>
      {{#title}}<h2  class="page-title" lang="{{language_code}}" dir="{{language_dir}}">{{{html}}}</h2>{{/title}}
      <!-- {{> browseSelect }} -->
      <div class="resultsnum">Showing results <span class="start"></span> - <span class="docslength"></span> of <span class="numfound"></span></div>
    </header>
    <div 
      class="widget" 
      data-name="items" 
      data-lazyLoad="0" 
      data-rows="10" 
      data-paginator="1" 
      data-script='{ "js" : [ "browse.min.js" ], "hbs" : [ { "items" : "items.hbs" } ] }'
    ></div>
    <div class="text-center">
      <div id="paginator" class="pagination"></div>
    </div>
  </main>
  {{> footer }}
  {{{ closure }}}
</body>
</html>